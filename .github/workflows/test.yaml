name: "test"

on:
  workflow_dispatch:
    inputs:
      image:
        description: image name and version
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
    - name: Copy
      uses: andstor/copycat-action@v3
      with:
        personal_token: ${{ secrets.PAT_TOKEN }}
        src_path: dist/
        src_branch: test/github-action-build
        dst_branch: test/github-actions-build
        dst_path: /.
        dst_owner: GJ-playgroud
        dst_repo_name: plugin-test
